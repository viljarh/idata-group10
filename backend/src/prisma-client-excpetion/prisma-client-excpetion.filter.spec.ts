import { PrismaClientExcpetionFilter } from './prisma-client-excpetion.filter';

describe('PrismaClientExcpetionFilter', () => {
  it('should be defined', () => {
    expect(new PrismaClientExcpetionFilter()).toBeDefined();
  });
});
